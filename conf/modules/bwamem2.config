
process {
  withName: 'BWAMEM2_MEM' {
        scratch = false
        ext.prefix = { "${meta.id}.${meta.lane}.bam" }
        ext.args = { "-R \'@RG\\tID:${meta.id}.${meta.lane}\\tPU:${meta.lane}\\tSM:${meta.id}\\tDS:${fasta}\'" } 
  }
}
